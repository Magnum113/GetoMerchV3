# Include in the HTTPS server block for admin.komui.ru.
gzip on;
gzip_vary on;
gzip_proxied any;
gzip_http_version 1.1;
gzip_min_length 1024;
gzip_comp_level 5;
gzip_types application/json application/problem+json;
